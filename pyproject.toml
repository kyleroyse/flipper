[project]
name = "flipper"
version = "0.1.0"
description = "Minimal runnable project scaffold"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
flipper = "main:main"

[tool.hatch.build.targets.wheel]
only-include = ["main.py", "README.md"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
